# 小潮瓜子 — 后台管理系统 & 前端完善 需求文档

> 版本：v1.1
> 日期：2026-04-28

---

## 一、项目背景

"小潮瓜子"是一个位于三门峡市湖滨区的传统炒货店铺，现有 4 个页面的静态品牌展示网站（首页、产品展示、关于我们、联系我们）。当前存在图片缺失、分类不完整、数据硬编码等问题。现需开发配套后台管理系统，并同步完善前端展示。

---

## 二、技术栈

| 层级 | 技术 | 选型理由 |
|------|------|----------|
| 后端框架 | **Node.js + Express** | 前后端统一 JavaScript，现代项目主流，简历加分 |
| 数据库 | **SQLite** (better-sqlite3) | 零配置，文件即数据库，小项目够用 |
| 前端 | **原生 HTML + CSS + JS** → 后期可升级 | 现有项目保持兼容，快速出成品 |
| 后台管理 | **EJS 模板** | 服务端渲染，简单直接 |
| 部署 | **香港轻量云服务器 + Nginx + PM2** | 真上线，真服务器，简历硬核 |
| HTTPS | **acme.sh 免费证书** | 安全标配 |

---

## 三、功能需求

### 3.1 前端完善

| 问题 | 处理方式 |
|------|----------|
| 产品图片缺失 | 补齐 `images/产品图片/缩略图/` 目录 |
| 产品数据硬编码 | 改为从后端 API 动态加载 |
| 首页轮播图过少 | 通过后台管理上传补充 |
| 空分类板块 | 去掉"休闲零食"空板块 |
| 店铺/资质图片缺失 | 通过后台管理上传补充 |

### 3.2 后台管理系统

#### 3.2.1 登录认证
- 管理员账号密码登录
- Session/Cookie 维持登录态
- 未登录自动跳转登录页
- **安全措施**：密码 bcrypt 哈希、会话超时

#### 3.2.2 产品管理
- **产品列表**：表格展示，支持搜索和分类筛选
- **新增产品**：名称、价格、分类、描述、图片上传、排序序号、上架状态
- **编辑产品**：修改信息，替换图片
- **删除产品**：确认弹窗后删除
- **分类**：瓜子(melon) / 坚果(nut) / 花生(peanut)

#### 3.2.3 轮播图管理
- 首页轮播图：上传、删除、排序
- 产品页轮播图：上传、删除、排序
- 店铺图片轮播：上传、删除
- 资质图片：上传、删除

#### 3.2.4 基础信息管理
- 店铺地址、联系电话、营业时间
- 品牌故事文案
- 微信二维码图片上传

#### 3.2.5 订单管理
- **本期不做**。店主目前通过微信接单，后续需要可加。

---

## 四、页面结构

### 前端页面（现有）

| 页面 | 路由 | 说明 |
|------|------|------|
| 首页 | `/index.html` | 默认入口 |
| 产品展示 | `/products.html` | 产品分类展示 |
| 关于我们 | `/about.html` | 品牌故事、店铺图片、资质 |
| 联系我们 | `/contact.html` | 电话、微信、地址、营业时间 |

### 后台管理页面

| 页面 | 路由 | 说明 |
|------|------|------|
| 登录页 | `/admin/login` | 管理员登录 |
| 控制台 | `/admin` | 概览仪表盘 |
| 产品管理 | `/admin/products` | 产品 CRUD |
| 轮播图管理 | `/admin/carousels` | 四类轮播图维护 |
| 基础设置 | `/admin/settings` | 店铺信息配置 |

---

## 五、数据表设计

### products 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键自增 |
| name | TEXT | 产品名称 |
| price | REAL | 价格 |
| category | TEXT | 分类：melon/nut/peanut |
| description | TEXT | 描述 |
| image | TEXT | 图片存储路径 |
| sort_order | INTEGER | 排序序号 |
| status | INTEGER | 1-上架 0-下架 |
| created_at | TEXT | ISO 时间 |
| updated_at | TEXT | ISO 时间 |

### carousels 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键自增 |
| type | TEXT | 分类：home/product/store/qualification |
| image | TEXT | 图片路径 |
| sort_order | INTEGER | 排序序号 |
| created_at | TEXT | ISO 时间 |

### settings 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键自增 |
| key | TEXT UNIQUE | 配置键名 |
| value | TEXT | 配置值 |
| updated_at | TEXT | ISO 时间 |

### admins 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键自增 |
| username | TEXT UNIQUE | 用户名 |
| password_hash | TEXT | bcrypt 哈希 |
| created_at | TEXT | ISO 时间 |

---

## 六、API 接口

### 公开接口

```
GET  /api/products          → 产品列表（支持 ?category=melon 筛选）
GET  /api/products/:id      → 单个产品详情
GET  /api/carousels         → 轮播图列表（支持 ?type=home 筛选）
GET  /api/settings          → 店铺基础设置
```

### 管理端接口（需登录）

```
POST   /api/admin/login       → 登录
POST   /api/admin/logout      → 退出
GET    /api/admin/me          → 当前登录状态
POST   /api/admin/products    → 新增产品
PUT    /api/admin/products/:id → 编辑产品
DELETE /api/admin/products/:id → 删除产品
POST   /api/admin/carousels   → 新增轮播图
DELETE /api/admin/carousels/:id → 删除轮播图
PUT    /api/admin/carousels/:id/sort → 更新排序
GET    /api/admin/stats       → 控制台统计数据
POST   /api/admin/upload      → 上传图片
PUT    /api/admin/settings    → 更新设置
```

---

## 七、安全措施

| 措施 | 实现方式 |
|------|----------|
| 密码保护 | bcrypt 哈希，不存明文 |
| 会话管理 | express-session + 随机密钥 |
| 登录校验 | 中间件拦截未授权请求 |
| SQL 注入防护 | 参数化查询（better-sqlite3） |
| XSS 防护 | EJS 模板自动转义输出 |
| CSRF 防护 | SameSite cookie |
| 上传校验 | 限制文件类型（jpg/png/webp）和大小（≤5MB） |
| HTTPS | acme.sh 免费证书部署 |
| 进程守护 | PM2 保活，崩溃自启 |

---

## 八、部署方案

```
香港轻量云服务器（腾讯云 24元/月）
  ├── Nginx（反代静态文件 + 转发 API 请求）
  ├── Node.js + Express（PM2 托管）
  ├── SQLite 数据库文件
  └── acme.sh（自动续期 HTTPS 证书）
```

---

## 九、开发计划

| 阶段 | 内容 | 产出 |
|------|------|------|
| 第一阶段 | 项目脚手架 + 数据库 + API | 后端能跑，接口可调 |
| 第二阶段 | 后台管理页面 | 登录、产品、轮播图、设置管理 |
| 第三阶段 | 前端完善 | 产品数据 API 化、图片补齐 |
| 第四阶段 | 部署上线 | 云服务器部署，HTTPS 配好 |

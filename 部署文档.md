# 小潮瓜子 — 部署文档

## 环境要求

- **服务器**：Linux（Ubuntu 20.04+ 推荐）
- **Node.js**：v18+
- **Nginx**：用于反向代理和静态文件服务

---

## 一、服务器基础配置

```bash
# 更新系统
apt update && apt upgrade -y

# 安装 Node.js（如已安装可跳过）
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs
node -v  # 确认版本
```

---

## 二、上传项目文件

```bash
# 在服务器上创建目录
mkdir -p /var/www/xiaochao-guazi

# 从本地上传（在本地电脑执行）
scp -r /本地项目路径/* root@你的服务器IP:/var/www/xiaochao-guazi/
```

或使用 WinSCP 等 SFTP 工具上传。

---

## 三、安装依赖并启动

```bash
cd /var/www/xiaochao-guazi
npm install --production

# 首次启动（会自动初始化数据库和管理员账号）
node app.js
```

确认能访问 `http://你的IP:3000` 正常后，按 Ctrl+C 停止，进行下一步。

---

## 四、PM2 进程守护

```bash
# 安装 PM2
npm install -g pm2

# 启动项目
pm2 start app.js --name xiaochao-guazi

# 设置开机自启
pm2 startup
pm2 save
```

常用 PM2 命令：
- `pm2 status` — 查看运行状态
- `pm2 logs xiaochao-guazi` — 查看日志
- `pm2 restart xiaochao-guazi` — 重启
- `pm2 stop xiaochao-guazi` — 停止

---

## 五、Nginx 反代 + HTTPS

### 安装 Nginx

```bash
apt install -y nginx
systemctl start nginx
systemctl enable nginx
```

### 配置 Nginx

创建配置文件 `/etc/nginx/conf.d/xiaochao-guazi.conf`：

```nginx
server {
    listen 80;
    server_name 你的域名或IP;

    client_max_body_size 10m;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

```bash
nginx -t          # 测试配置
systemctl reload nginx  # 重载配置
```

### 配置 HTTPS（acme.sh）

```bash
# 安装 acme.sh
curl https://get.acme.sh | sh

# 申请证书（需要域名已解析到服务器）
~/.acme.sh/acme.sh --issue -d 你的域名 --nginx

# 安装证书
~/.acme.sh/acme.sh --install-cert -d 你的域名 \
  --key-file /etc/nginx/ssl/你的域名.key \
  --fullchain-file /etc/nginx/ssl/你的域名.cer \
  --reloadcmd "systemctl reload nginx"
```

然后在 Nginx 配置中加上 HTTPS 服务器块。

---

## 六、防火墙配置

```bash
# 开放端口
ufw allow 22/tcp     # SSH
ufw allow 80/tcp     # HTTP
ufw allow 443/tcp    # HTTPS
ufw enable
```

---

## 七、验证部署

- `http://你的域名/admin/login` — 后台管理登录页
- `http://你的域名/` — 前台网站
- 默认管理员账号：`admin` / `admin123`

---

## 八、日常维护

```bash
# 更新代码
cd /var/www/xiaochao-guazi
git pull  # 如果用 git 管理
# 或重新上传文件
pm2 restart xiaochao-guazi

# 备份数据库（重要！）
cp /var/www/xiaochao-guazi/data/database.sqlite /备份路径/

# 查看日志
pm2 logs xiaochao-guazi
```

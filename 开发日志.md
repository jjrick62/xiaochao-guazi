# 小潮瓜子 — 开发日志

> 说明：本文档记录本项目中 Claude Code（洒家）所做的一切工作，方便以后回来查看项目全貌。

---

## 一、项目总览

| 项目 | 内容 |
|------|------|
| 项目名 | 小潮瓜子品牌展示网站 + 后台管理系统 |
| 技术栈 | Node.js + Express + SQLite (sql.js) + EJS 模板 |
| 前台页面 | 首页、产品展示、关于我们、联系我们 |
| 后台管理 | 登录、控制台、产品管理、轮播图管理、基础设置 |
| 数据库 | SQLite（文件型，无需安装数据库服务器） |
| 部署方式 | Nginx 反代 + PM2 进程守护 + acme.sh HTTPS |

---

## 二、项目搭建与修改

### 新增后端文件

| 文件/目录 | 说明 |
|-----------|------|
| `app.js` | Express 主入口，启动服务器 |
| `db.js` | 数据库初始化、建表、种子数据、查询工具函数 |
| `routes/api.js` | 公开 API（产品列表/详情、轮播图、设置） |
| `routes/admin.js` | 管理端 API（CRUD、登录认证）+ 后台页面路由 |
| `middleware/auth.js` | 登录鉴权中间件 |
| `views/` | EJS 模板文件夹 |
| `views/login.ejs` | 管理员登录页 |
| `views/dashboard.ejs` | 控制台仪表盘 |
| `views/products.ejs` | 产品管理（CRUD） |
| `views/carousels.ejs` | 轮播图管理（四类） |
| `views/settings.ejs` | 基础设置（含修改密码） |
| `views/partials/header.ejs` | 后台页面通用头部 |
| `public/admin-style.css` | 后台管理页面样式 |
| `public/uploads/` | 图片上传存储目录 |
| `data/` | SQLite 数据库文件存放目录（自动创建） |

### 原有前端修改

| 文件 | 修改内容 |
|------|----------|
| `js/script.js` | 删除硬编码产品数据，改为从 `/api/products` 异步加载；首页品牌故事改为从 API 加载；联系页面信息改为 API 加载；轮播图数据也改为 API 加载 |
| `products.html` | 删除了"休闲零食"(#snacks)空分类板块，删除了侧边栏中对应的导航项 |
| `css/style.css` | 未作修改，保持原有样式 |

### 原有文件保留

`index.html`、`about.html`、`contact.html` 结构未改动，数据改为通过 API 动态加载。

---

## 三、数据库设计

共 4 张表：

### products（产品表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| name | TEXT | 产品名称 |
| price | REAL | 价格 |
| category | TEXT | 分类：melon(瓜子)/nut(坚果)/peanut(花生) |
| description | TEXT | 产品描述 |
| image | TEXT | 图片路径（相对路径，如 /uploads/xxx.jpg） |
| sort_order | INTEGER | 排序序号 |
| status | INTEGER | 1=上架 0=下架 |
| created_at | TEXT | 创建时间 |
| updated_at | TEXT | 更新时间 |

### carousels（轮播图表）

| 字段 | 说明 |
|------|------|
| type | home(首页轮播)/product(产品页轮播)/store(店铺图片)/qualification(资质图片) |
| image | 图片路径 |
| sort_order | 排序序号 |

### settings（系统设置表）

key-value 结构，存储：店铺地址、电话、营业时间、品牌故事、微信二维码路径。

### admins（管理员表）

预置默认管理员：`admin` / `admin123`（密码 bcrypt 哈希）。

---

## 四、API 接口清单

### 公开接口（无需登录）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/products` | 产品列表，支持 `?category=melon` 筛选 |
| GET | `/api/products/:id` | 产品详情 |
| GET | `/api/carousels` | 轮播图列表，支持 `?type=home` 筛选 |
| GET | `/api/settings` | 获取所有店铺设置 |

> **注意**：管理端 API 路径统一以 `/admin/api/` 开头，与后台页面路由（`/admin/products` 等）不冲突。

### 管理端接口（需登录）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/admin/api/login` | 管理员登录 |
| POST | `/admin/api/logout` | 退出 |
| GET | `/admin/api/me` | 获取当前登录状态 |
| GET | `/admin/api/stats` | 控制台统计数据 |
| GET | `/admin/api/products` | 管理端产品列表 |
| POST | `/admin/api/products` | 新增产品 |
| PUT | `/admin/api/products/:id` | 编辑产品 |
| DELETE | `/admin/api/products/:id` | 删除产品 |
| POST | `/admin/api/carousels` | 上传轮播图文件 |
| POST | `/admin/api/carousels/url` | 新增轮播图（指定URL） |
| DELETE | `/admin/api/carousels/:id` | 删除轮播图 |
| PUT | `/admin/api/carousels/:id/sort` | 更新轮播图排序 |
| POST | `/admin/api/upload` | 单文件上传 |
| POST | `/admin/api/upload-multiple` | 多文件上传 |
| PUT | `/admin/api/settings` | 更新店铺设置 |
| PUT | `/admin/api/change-password` | 修改管理员密码 |

### 后台管理页面

| 路径 | 页面 |
|------|------|
| `/admin/login` | 登录页 |
| `/admin` | 控制台 |
| `/admin/products` | 产品管理 |
| `/admin/carousels` | 轮播图管理 |
| `/admin/settings` | 基础设置 |

---

## 五、安全措施

| 措施 | 实现方式 |
|------|----------|
| 密码保护 | bcryptjs 哈希，不存明文 |
| 登录态 | express-session + httpOnly cookie，24小时过期 |
| 鉴权中间件 | `requireAdmin` 拦截未登录请求 |
| SQL 注入防护 | 参数化查询（占位符 `?`），所有 SQL 都走 `db.run('sql', [params])` |
| XSS 防护 | EJS 模板自动转义输出（`<%= %>`） |
| CSRF 防护 | SameSite cookie (`sameSite: 'lax'`) |
| 文件上传限制 | multer 限制 5MB 大小，仅允许 jpg/png/webp/gif |
| 会话密钥安全 | 服务启动时生成随机密钥，不硬编码 |

---

## 六、种子数据

首次启动时自动初始化：

- **管理员**：`admin` / `admin123`
- **产品**：12 个示例产品（5 瓜子 + 3 花生 + 4 坚果）
- **设置**：店铺地址、电话、营业时间、品牌故事等默认值

---

## 七、启动方式

```bash
# 开发
npm run dev     # node --watch app.js（自动重启）

# 生产
npm start       # node app.js
```

访问：
- 前台网站：`http://localhost:3000`
- 后台管理：`http://localhost:3000/admin/login`

---

## 八、部署要点

详见 `部署文档.md`，关键步骤：

1. 服务器装 Node.js + Nginx
2. 上传项目文件，`npm install --production`
3. PM2 启动并保活：`pm2 start app.js --name xiaochao-guazi`
4. Nginx 反代到 localhost:3000
5. acme.sh 配置 HTTPS 证书
6. 防火墙开 80/443 端口
7. **定期备份** `data/database.sqlite`

---

## 九、后续计划

### 线上下单 + 微信支付

已完成微信支付商户号申请的前提确认（有营业执照），后续开发内容包括：

| 功能 | 说明 |
|------|------|
| 下单系统 | 前台选产品 → 填写信息 → 生成订单 |
| 微信支付接入 | 调用微信支付 JSAPI/H5 接口 |
| 订单管理 | 后台查看所有订单、修改状态 |
| 订单状态流转 | 待付款 → 已付款 → 已发货 → 已完成 |

> **前提**：需申请微信支付商户号（凭营业执照在 pay.weixin.qq.com 申请），拿到商户号和 API 密钥后洒家即可开发。

---

## 十、已知说明

- 图片管理交给后台，老板可自行上传，前端不做硬补图
- 产品数据从 API 加载，修改后台后前台自动生效
- 上传的图片存储在 `public/uploads/` 目录
- 数据库文件在 `data/database.sqlite`，迁移服务器时别忘了拷贝
